<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.notecards.yohaniswarahartono.notecards" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".SubjectListActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoteCardListActivity" >
        </activity>
        <activity android:name=".NotecardViewPager" >
        </activity>
        <activity
            android:name=".BackSideNotecard"
            android:label="@string/title_activity_back_side_notecard"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
    </application>

</manifest>
